# Harness Evaluation Checklist

Generated 2026-09-12 by running the `harness-eval` skill (Tech Leads Club), Track A+B+C, against every
`extended/<skill>/` override in this repo plus the templates and local skills it surfaced.
Premise for this pass: the vendor (Tech Leads Club) skills these overrides sit on top of were freshly updated,
so the question for every row is whether the override/template still earns its place now that the base has moved.

This is a **checklist, not a changelog**. In `Overridden Skills` and `Templates` below, **one row is one
actionable item**, not one row per file — where a file's run surfaced more than one action, it's split across
multiple numbered rows (shared `#` and name) so each can be tracked and referenced independently; a file needing
no action gets exactly one `Rejected` row so it stays traceable. Work items one at a time, flip `Status` as you
go, and use `Description` + `How to Fix` + `How to validate` so a later pass doesn't need to re-run harness-eval
from scratch to remember why a row exists.

Raw run output for every row lives under `.harness-eval/runs/<run-id>/` in this repo (report-only; harness-eval
never auto-applies Ship/Slim/Mixed verdicts).

## Known tooling limitations (read before trusting a trap-FAILED row)

`harness-eval`'s calibration ("trap") plants and its deterministic fan-in scanner (`slim_fanin.py`) assume a
repo layout with `AGENTS.md` and `.agents/skills/` / `.cursor/skills/` / `.claude/skills/` skill trees. This
repo has none of those — it uses `CLAUDE.md` + top-level `skills/` and `extended/` — so:

- **Track B / Track C trap gates failed on several rows below** purely because the calibration plants cite
  paths that don't exist here, not because of anything wrong with the file being evaluated. A failed trap
  discards the Ship/Slim band for that run — those rows carry **uncertified** signal only, flagged as such.
- **`slim_fanin.py` itself returns zero citers even for files with real callers**, because its scan is
  hardcoded to `.agents/skills/**` / `.cursor/skills/**` / `.claude/skills/**`. Every run in this sweep worked
  around this by instructing judges to manually `grep` the real repo — but at least one run
  (`templates/test-execution-scope.md`) shows even manually-instructed judges can still undercount callers, so
  a Slim/Mixed verdict should always be independently spot-checked with a fresh
  `grep -rl "<template-name>" --include="*.md" .` before acting on it.
- **This document itself was incidentally read by judges** during at least one run (a caller-grep matched
  `docs/HARNESS-EVALUATION.md`). No verdict was actually changed by it so far, but future runs should
  explicitly exclude this file from judge greps/reads to avoid a judge cribbing a prior run's answer instead of
  deriving its own (see `Templates` item 16).

Until harness-eval's plant templates and fan-in scanner are adapted to this repo's actual conventions, treat
any Ship/Slim verdict on a trap-FAILED row as a lead to manually confirm, not a certified action.

## Glossary — what each column means

| Column | Meaning |
| --- | --- |
| # | Sequential reference number for one concrete action item within its table (`Overridden Skills`, `Templates`, `Skills`, and `Root Context Files` each number independently). Use this to point at a specific item in conversation. |
| Name of the skill / template | The `extended/<skill>/` override, `templates/*.md`, or `skills/<name>/` file this row's action item applies to. The same name repeats across consecutive rows when a file has more than one action item. |
| Status | `Pending` (not started) · `In Progress` (being worked) · `Done` (recommended action applied and validated) · `Rejected` (evaluated, consciously decided not to act — including "no action needed, file confirmed fine"). |
| Track A | Deterministic correctness check (harness-eval script, ~0 model tokens): do the paths/commands this file cites actually exist? Highest certainty of the three tracks. `—` when not relevant to this specific action item. |
| Track B | Redundancy check (dual LLM judges + calibration plants): would an agent already know this without the file (i.e. is it duplicated elsewhere in the repo)? Only certified (`Ship`) if the trap gate passed; a failed trap means "uncertified" — informative, not actionable on its own. `—` when not relevant to this specific action item. |
| Track C | Usefulness check (dual LLM judges + plants + fan-in): if this whole file were deleted, would agent behavior actually change? `Keep-core` = essential, `Slim` = compressible/removable, `Mixed` = partly each, `Hold` = judges disagreed or trap failed. Gated by a fan-in check for other files that hard-load this one. `—` when not relevant to this specific action item. |
| Bottom line | One-sentence statement of this specific action item, in plain language. |
| Description | Full narrative: the specific findings, quoted evidence (file:line where relevant), and reasoning — detailed enough that evaluating this row later doesn't require re-reading the original harness-eval run. |
| How to Fix | Concrete edit(s) to make to resolve this item — what to change, where, and to what. |
| How to validate | Concrete, mechanical steps to confirm the recommended action actually resolved the finding once `Status` moves to `Done`. |

## Overridden Skills

| # | Name of the skill / template | Status | Track A | Track B | Track C | Bottom line | Description | How to Fix | How to validate |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `docs-writer` | Pending | — | Uncertified (trap FAILED) — dual REDUNDANT-GENERAL on C003 | — | Confirm and cut C003 as a near-duplicate of `templates/token-efficiency-rules.md`'s own scope list. | C003 ("apply these rules to all generated content: reference files, SKILL.md files, docs/ files...") scored REDUNDANT-GENERAL by both Track B judges in `.harness-eval/runs/2026-09-12-docs-writer-override/07-agreement.md`, but downgraded to Hold purely because the trap gate failed (calibration plants cited `.agents/skills/workflow-tips/SKILL.md`/`AGENTS.md`, which don't exist in this repo), not by judge disagreement — the redundancy signal itself is real, just uncertified this run. | Manually diff C003's wording against `templates/token-efficiency-rules.md`'s frontmatter `description`/scope list; if it's a genuine restatement, delete C003 from `extended/docs-writer/SKILL.md`. | Confirm the sentence is gone via diff; re-run `track_a_correctness.py` seeded on the file — should stay clean (0 broken, unchanged). |
| 2 | `docs-writer` | Pending | — | — | — | Add a one-line precedence note between this override's token-efficiency rules and the vendor's style-guide mandate. | Vendor `docs-writer/SKILL.md`'s Step 3 unconditionally mandates the human-voice `references/style-guide.md` ("adhere to... for all docs"), with no stated precedence over this override's separate token-efficiency rules for agent-facing `.md` files. The two don't flatly contradict today, but nothing says which wins if a future edit makes them clash. | Add a one-line note in `extended/docs-writer/SKILL.md` stating token-efficiency rules take precedence over `style-guide.md`'s voice/tone sections when writing agent-facing `.md` files. | Confirm the note doesn't contradict the vendor's Step 3 wording, and a human-facing doc (e.g. a README) is unaffected. |
| 3 | `mermaid-studio` | Pending | — | — | — | Add an automated fill/text-contrast check — the black-text rule is prose-only and has already failed once. | The override's "pair every `fill:`/`$bgColor` with explicit black text" rule has zero automated enforcement (`scripts/validate.mjs` has no color/contrast checks). This repo's own root `CLAUDE.md` says a script is the right fix "especially when a prose rule... has demonstrably failed to hold" — and this one has (a prior invisible-text incident; the vendor's own color bug is independently re-verified as still present in `references/themes.md`). | Add a check to `extended/mermaid-studio/scripts/render-c4-fixed.mjs` (or a new script) that scans generated mermaid theme blocks for a light `fill`/`mainBkg` lacking a paired dark `color`/`fontColor`, and warns or fails the render. | Run the check against a test block with a light fill and no paired dark text color — confirm it flags; run against a correctly-paired block — confirm it passes. |
| 4 | `skill-architect` | Pending | — | Ship (certified) — C044/C048/C061–C085 | MIXED (certified) — CUT per `11-mixed-apply.md` | Replace Extension 2's inline frontmatter YAML block with a link to `templates/extension-frontmatter.md`. | `extended/skill-architect/SKILL.md` Extension 2 (~lines 142–156, "Frontmatter template for an extension") inlines the entire YAML block near-verbatim instead of linking, despite the same file correctly linking to 3 sibling templates elsewhere. Independently corroborated by the separate `templates/extension-frontmatter.md` run (Templates item 2), which confirmed via manual grep this is the only skill-tree caller that duplicates instead of pointing. Certified — both tracks trap-passed. | Delete the inline YAML block; replace with a one-line link ("See [Extension Frontmatter](../../templates/extension-frontmatter.md)"), matching how Extensions 1/3/4 already link out. | Diff the section — confirm no YAML block remains, only a link. Confirm the template still covers every field the deleted block had. |
| 5 | `skill-architect` | Pending | — | Ship (certified) — C061–C085 (~20 claims) | MIXED (certified) — CUT per `11-mixed-apply.md` | Replace Extension 3's output-rules bullets + Phase-4 checklist with a link to `templates/token-efficiency-rules.md`. | Extension 3 restates `templates/token-efficiency-rules.md`'s Remove/Trim/Preserve rules almost verbatim (~lines 182–198) despite linking to the template one line later. Independently corroborated by the separate `templates/token-efficiency-rules.md` run (Templates item 14), which additionally caught that the inline copy has already **drifted** from the source (template: `# PHP 8.3+`; this copy: `# PHP 8.4+`). | Delete the inline checklist bullets; replace with a link to `templates/token-efficiency-rules.md`, mirroring Extension 4's link to `agent-wait-protocol.md`. Resolve the PHP 8.3-vs-8.4 drift before deleting — don't just drop the discrepancy silently. | Diff the section — confirm only a link remains. `grep -n "PHP 8\."` across both files — confirm one version number exists repo-wide after the edit. |
| 6 | `skill-architect` | Pending | — | — | Mixed apply (fix, not cut) | Point Extension 2 step 5 at `README.md`'s skill table instead of the nonexistent `AGENTS.md`. | Extension 2 step 5 says "update `AGENTS.md` to annotate the parent skill entry" — `AGENTS.md` doesn't exist in this repo. The real convention is `README.md`'s skill table, which doesn't even carry this annotation for `skill-architect`'s own entry yet — the instruction has never been followed, even on itself. | Change the instruction to point at `README.md`'s skill table; add the missing annotation for `skill-architect` itself to `README.md`. | Confirm `README.md`'s skill table lists `skill-architect` with the annotation this step calls for, and the step no longer references `AGENTS.md`. |
| 7 | `skill-architect` | Pending | Clean | — | — | Renumber Extension 3's injected "2.4" step — it collides with the base skill's own current "2.4." | Extension 3 injects "2.4 — Reference File Design" into Phase 2, but the current base vendor `skill-architect` skill already has its own "2.4 — Plan Progressive Disclosure" at that exact position — two different "2.4"s when an agent follows the phases in order. Not caught by any formal harness-eval track; found by direct comparison against the current vendor `SKILL.md`. | Renumber Extension 3's injected step to "2.5" (or the next free slot), folding Extension 4's own "2.5" down by one if needed so no two steps share a number. | Read the merged `SKILL.md` + `SKILL.extended.md` phase list in order — confirm every step number is unique and sequential. |
| 8 | `skill-architect` | Pending | — | Ship (certified) — C036–C038 | — | (Lower priority) Trim the boundary rule duplicating root `CLAUDE.md`'s own Skill Modification Rules. | C036–C038 ("never edit global skills directly, use `extended/`") duplicate this repo's own root `CLAUDE.md` "Skill Modification Rules" section almost exactly — against project-level instructions (always loaded), not the vendor skill. | Cut the restated rule, or shorten it to a one-line pointer at root `CLAUDE.md`'s Skill Modification Rules section. | Confirm the trimmed section still reads standalone and nothing else assumed the deleted text. |
| 9 | `tlc-spec-driven` | Pending | 2 broken cites | — | — | Fix the two dead `PROJECT_DETAILS.md` cites in `references/coding-principles.md`. | Cites `docs/codebase/PROJECT_DETAILS.md` and `docs/PROJECT_DETAILS.md` — both dead legacy paths (see `[[project_docs_codebase_convention]]`); current convention is `docs/codebase/STACK.md`/`PROJECT.md`. Confirmed via Track A, harness-eval's deterministic correctness check — unconditional, not subject to any trap-gate caveat. | Replace both cites with the correct current path(s) (`STACK.md`/`PROJECT.md`, whichever the sentence means). | Re-run `track_a_correctness.py` seeded on `extended/tlc-spec-driven/SKILL.md` — should drop from 2 to 0 broken. |
| 10 | `tlc-spec-driven` | Pending | — | Uncertified (trap FAILED) — dual REDUNDANT-GENERAL | — | Consolidate the duplicate "Code Comments" guidance into one canonical location. | `references/coding-principles.md`'s "Code Comments" section and `references/coding-guidelines/best-practices-coding-guidelines.md`'s "## Comments" section duplicate each other near-verbatim — flagged independently by both Track B judges here and by both Track C judges on the separate `best-practices-coding-guidelines.md` surface (item 11). Also resembles the user's own global `CLAUDE.md` "No explanatory comments" rule, though that specific comparison is uncertified (trap failed). | Pick one canonical location (recommend `coding-principles.md`) and delete or point to it from `best-practices-coding-guidelines.md`. | `grep -rn "Code Comments\|## Comments"` across the override's `references/` tree — the rule's substance should appear once, not twice. |
| 11 | `tlc-spec-driven` | Pending | — | — | MIXED (uncertified, trap FAILED, but corroborated by both judges) | Apply the Mixed cut on `best-practices-coding-guidelines.md`: keep the Pre-Completion Checklist, cut the theory block. | Both Track C judges independently landed on MIXED: **KEEP** the gated 10-item Pre-Completion Checklist (`coding-principles.md` mandates running it before marking any task complete — behavior-changing); **CUT** the ~80-line SOLID/DRY/KISS/YAGNI/etc. theory block (stable textbook theory, compressible) plus its trailing "## Comments" section (see item 10). | Delete/compress the theory-block prose to a bare list of principle names if kept at all; delete the trailing "## Comments" section per item 10's consolidation. Leave the Pre-Completion Checklist untouched. | Confirm the Pre-Completion Checklist still reads standalone and complete after the theory block is removed; re-run Track A — should stay clean. |
| 12 | `tlc-spec-driven` | Pending | — | Hold → certified via cross-run corroboration | Keep-core (certified elsewhere) | Replace `SKILL.md`'s duplicated "Phase Models" table with a pointer to `templates/subagent-models.md`. | `SKILL.md`'s "Phase Models" table and its "the model never varies with whether a human is reviewing" line restate `templates/subagent-models.md`'s own tier matrix and `human_review` invariant. Originally Hold-by-trap-failure in this override's own run; **now certified** by the separate `templates/subagent-models.md` run (item 12 below, trap PASS), which confirmed the overlap and noted `tlc-spec-driven` does add two things the template lacks: the rationale for Tasks sitting a tier below the rest, and an orchestrator-vs-direct-invocation distinction. | Replace the duplicated table/line with a link to `templates/subagent-models.md`, preserving the two non-duplicated additions as short standalone notes rather than deleting them with the table. | Confirm the tier values no longer appear inline (only via the link), and the two preserved notes still read coherently on their own. |
| 13 | `tlc-spec-driven` | Pending | — | — | — | Add a JS/Node `coding-guidelines` file — the "Stack-Specific Style" convention promises one per stack but only ships PHP. | `coding-principles.md`'s "Stack-Specific Style" section says to load a matching `coding-guidelines/<language>-*.md` file per detected stack, but only `php-coding-guidelines.md` exists. This project's own stack is JS/Node (`docs/codebase/STACK.md`), so the section has nothing to load for the very project it lives in. Flagged independently by both Track C judges as a qualitative gap outside harness-eval's formal tracks. | Author `extended/tlc-spec-driven/references/coding-guidelines/js-coding-guidelines.md` following the same structure as `php-coding-guidelines.md`. | Confirm the new file mirrors the PHP one's structure, and `coding-principles.md`'s stack-detection logic resolves to it for this repo. |
| 14 | `tlc-spec-driven` | Pending | — | — | Keep-core (but dead weight for this repo specifically) | (Human call, no forced action) Decide whether `php-coding-guidelines.md` belongs in this repo's active set. | Scored dual Keep-core, but both judges noted its own stack-detection logic can never select it here (JS/Node stack) — still valid as a template for a future PHP project, just inert here. Not a Slim candidate; purely a scope call. | No forced edit. If keeping only actually-usable references active, move `php-coding-guidelines.md` to a separate "templates for future stacks" location; otherwise leave as-is. | If moved, confirm `coding-principles.md`'s stack-detection logic no longer expects to find it at the original path. |

## Templates

| # | Name of the skill / template | Status | Track A | Track B | Track C | Bottom line | Description | How to Fix | How to validate |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `templates/agent-wait-protocol.md` | Pending | Clean | Hold (certified, disagree) — C012 | Keep-core (certified) | (Optional, low priority) Trim one editorial sentence (C012) if this file is touched for any other reason. | C012 ("the rules sit above this section because...") is editorial self-justification for ordering, not an operational rule — Judge1 KEEP-CAVEAT vs Judge2 REDUNDANT-GENERAL. Minor, non-blocking; all 8 real callers (9 citation sites) are otherwise confirmed clean bare pointers with zero drift risk — this template centralizes measurement-backed guidance (183 wasted polling calls, 345 no-op calls burning 77.6M tokens, 23 tool mis-substitutions across real incidents). | Delete or shorten C012 to a fragment of the surrounding sentence. | Confirm the section still reads coherently without a dangling "because" clause. |
| 2 | `templates/extension-frontmatter.md` | Rejected | Clean | Hold (uncertified, trap FAILED) | Hold (uncertified, trap FAILED) | No independent action on this template — the fix belongs to its caller (see Overridden Skills item 4). | This run confirmed the template itself is sound (0 Ship, all real claims Review/Hold, nothing redundant). The only actionable finding — `extended/skill-architect/SKILL.md` Extension 2 inlining instead of linking — is the same edit already captured as Overridden Skills item 4; do not duplicate the fix here. | — (see Overridden Skills item 4) | — (see Overridden Skills item 4) |
| 3 | `templates/gh-account-resolution.md` | Rejected | Clean | Uncertified (trap FAILED) — unanimous dual-KEEP on all 9 claims | Keep-core (certified) | No action — keep as-is. | Sole source of truth for a genuinely non-obvious `gh` CLI gotcha (global mutable auth state). 4 real callers (1 mandatory in `build-feature`, 3 opt-in), none duplicate its content; every clause maps to a specific `gh` CLI gotcha not stated in `gh`'s own help text. | — | — |
| 4 | `templates/github-pr-review-mode.md` | Pending | Clean | Ship (certified) — 6 trivial claims | Keep-core (certified) | (Caller-side, optional) Trim 3 call sites that re-paste this template's anchor-rationale paragraph instead of linking. | `skills/code-review/SKILL.md:324`, `skills/tests-code-review/SKILL.md:308`, and `skills/complete-review/SKILL.md:180` each duplicate/paraphrase the anchor-mechanics + "13-35%/21k-102k tokens" cost-rationale paragraph before linking back to this template "for the full contract" — drift from the intended shared-template pattern. The template itself needs no change. | In each of the 3 files, replace the re-pasted paragraph with a short pointer to `templates/github-pr-review-mode.md`, keeping only what's specific to that caller. | `grep -c` a unique phrase from the anchor-rationale paragraph across the 3 files plus the template — should return 1 (the template) after the fix, not 4. |
| 5 | `templates/reference-file-naming-convention.md` | Pending | Clean | Review (certified) — 0 Ship | Hold (certified, disagree) | (Optional, low priority) Align `docs/codebase/CONVENTIONS.md`'s partial inline restatement with this template. | `docs/codebase/CONVENTIONS.md:47` partially inlines the naming pattern (one line) while omitting the exemption clause and examples — a minor drift risk. Out of this run's formal scope (a generated project doc), so not a certified finding, just an observation both judges independently made. The template's own single real caller (`extended/skill-architect/SKILL.md:173`) is already a correct bare pointer. | In `docs/codebase/CONVENTIONS.md`, replace the partial inline restatement with a link to the template, or add the missing exemption clause so the two don't drift. | Confirm `CONVENTIONS.md` and the template agree on the exemption clause, or that `CONVENTIONS.md` just links instead of restating. |
| 6 | `templates/reference-loading-constraint.md` | Pending | 2 broken cites | Review (certified) — C003/C004 Hold | Hold (certified, disagree — MIXED vs SLIM) | Fold the one non-duplicated rule into `coding-principles.md`, then delete this standalone template. | This template has exactly one real caller (`extended/tlc-spec-driven/references/coding-principles.md:37`), which already inlines the same STACK.md fallback chain before linking — the link is decorative. The only piece neither judge found duplicated anywhere is the "STACK.md missing → load generic only" edge case. Both judges converge on the root cause despite disagreeing on the Hold label. | 1) Add the "STACK.md missing → generic only" rule directly into `coding-principles.md` in place of the current link. 2) Delete the redundant fallback-chain restatement already at `coding-principles.md` lines 33–34 (keep one copy). 3) Delete `templates/reference-loading-constraint.md`. | Confirm `coding-principles.md` states the STACK.md fallback chain exactly once, and `grep -rl "reference-loading-constraint"` returns nothing. |
| 7 | `templates/reference-loading-constraint.md` | Pending | 2 broken cites | — | — | Fix (or drop, if item 6 lands first) the two dead `PROJECT_DETAILS.md` cites inside this template. | The template's own prose cites `docs/codebase/PROJECT_DETAILS.md` and `docs/PROJECT_DETAILS.md` as legacy fallback paths — both dead in this repo (same class of dead path as Overridden Skills item 9, a separate occurrence in a different file). Moot if item 6's deletion happens first. | If item 6 is done first, this resolves automatically. Otherwise, fix both paths to the current `docs/codebase/STACK.md`/`PROJECT.md` convention. | If fixed independently, re-run `track_a_correctness.py` seeded on this file — should drop to 0 broken. |
| 8 | `templates/reply-review-filter.md` | Rejected | Clean | Review (certified) — 13/13 dual-KEEP | Keep-core (certified) | No action — keep as-is. | Sole working copy of a correctness-critical GitHub API gotcha (review-thread replies are indistinguishable from real reviews to naive selectors), grounded in a specific verified repro (`flaviosv/applyr#16`). Both real callers (`complete-review`, `fix-review`) confirmed bare pointers deferring entirely to this template. | — | — |
| 9 | `templates/reply-review-filter.md` | Pending | — | — | — | (Process/meta) Sanity-check Track B's trap mechanism itself in future runs — Judge1 missed 2/4 plant families here. | The merge script's trap gate is keyed to Judge2 only, so this run technically passed — but Judge1 independently missed 2/4 plant families, more than Judge2's 1. Doesn't change this file's Keep verdict (both tracks agree regardless), but suggests the trap mechanism may be less reliable sweep-wide than per-run PASS/FAIL labels suggest. | Not a file edit — a process note for whoever next runs harness-eval on this repo: check Judge1's plant scores too, not just Judge2's, before trusting a "PASS." | N/A — methodology caution, not a code change. |
| 10 | `templates/subagent-dispatch-contract.md` | Rejected | Clean | Uncertified (trap FAILED) — 9/10 dual-KEEP | Keep-core (certified) | No action — keep as-is. | Strongest signal in the batch: 7 skills / 9+ call sites, all confirmed substantive appliers (each states its own completion condition/return shape, citing the template only for the shared field structure) — not bare pointers, not duplication. Confirmed no overlap with `agent-wait-protocol.md` or `subagent-models.md`. | — | — |
| 11 | `templates/subagent-models.md` | Rejected | Clean | Hold (certified, disagree) — C002–C004, C006–C007 | Keep-core (certified) | No independent action on this template — it's the sole complete cross-skill model matrix; downstream drift is the real issue (see item 12). | 12 real callers confirmed; most are bare pointers. The Hold cluster reflects a legitimate rubric-boundary disagreement about whether several *callers* still restate content instead of pointing — a caller-side problem, not a reason to edit this file. | — | — |
| 12 | `templates/subagent-models.md` | Pending | — | Hold (certified, disagree) | — | (Caller-side) Clean up `code-review`, `tests-code-review`, and `fix-review` restating this template's "hard facts about the Agent tool" instead of linking. | Judge1 flagged the "Two hard facts about the Agent tool" content (aliases-only, no-effort-param, `human_review` invariant, merge-conflict-on-sonnet) as REDUNDANT-GENERAL in these 3 files because it's restated near-verbatim rather than pointed-to; Judge2 disagreed on the label but not on the fact of the restatement. Same pattern as Overridden Skills item 12 (`tlc-spec-driven`'s Phase Models table). | In each of the 3 skills' `SKILL.md`, replace the restated "hard facts" text with a link to `templates/subagent-models.md`. | `grep -rn` a unique phrase from the "hard facts" text across the 3 files and the template — should exist once (the template), not four times. |
| 13 | `templates/test-execution-scope.md` | Rejected | 3 flagged (false positives — files exist under the globally-installed vendor skill, invisible to the repo-local scan) | Review (certified) — 27/27 dual-KEEP | MIXED — but **incorrect, do not apply** | **Do not apply this run's own `11-mixed-apply.md` CUT.** No action on the template — keep whole. | Both Track C judges recommended cutting the Tiers/Stop-rule/Delegating-scope sections as OVERLAP with `CLAUDE.global.md`, based on a fan-in check (automated and judge-manual alike) that undercounted callers to just 1. Manual re-verification found 7 real citers, including `skills/build-feature/SKILL.md:242` and `skills/fix-review/SKILL.md:31,94,205`, which cite this template directly (not `CLAUDE.global.md`'s condensed version) and depend on exactly the sections flagged for cutting. Also: this repo's `templates/` directory is symlinked as `~/.claude/templates/` (matching inode confirmed) — this file is load-bearing on every Claude Code session on this machine, not just this repo. | None — this is a "do not act" item. If Track C is re-run on this file, instruct judges to `grep -rl "test-execution-scope" --include="*.md" .` across the whole repo before scoring fan-in. | Confirm `11-mixed-apply.md` for this run is never executed. If re-judged, confirm the new fan-in check finds all 7 real citers before trusting any Slim/Mixed verdict. |
| 14 | `templates/token-efficiency-rules.md` | Pending | Clean | Ship (certified) — C004 | — | Cut the "reduce consecutive blank lines to one" bullet (C004). | Both Track B judges independently classified C004 as REDUNDANT-GENERAL, cost ≤1 — a trivially rediscoverable formatting nit. Certified: this run's trap gate passed (1/4 miss, within tolerance). Separately, `extended/skill-architect/SKILL.md` inlines most of this template's other rules instead of linking — see Overridden Skills item 5, not duplicated here. | Delete the bullet from `templates/token-efficiency-rules.md`. | `grep -rn "consecutive blank lines"` — confirm the rule no longer appears (or only appears where intentionally kept elsewhere). |
| 15 | `templates/unsupported-tech-stack-alert.md` | Pending | Clean | Ship (certified) — C005/C008 UI filler | Slim → CUT (certified, unanimous) | Delete `templates/unsupported-tech-stack-alert.md` — zero real callers anywhere in the repo. | Confirmed via 3 independent manual greps: no `skills/*/SKILL.md`, `extended/*/SKILL.md`, or reference file links to this template. The two skills that would logically use it (`code-review`, `tests-code-review`) each implement their own inline, non-blocking fallback instead — the opposite, graceful-degrade design already covered by the sibling `templates/reference-loading-constraint.md` (items 6–7), which *is* actually wired in. Superseded/orphaned, not merely unused. | Delete `templates/unsupported-tech-stack-alert.md`. | `grep -rl "unsupported-tech-stack-alert" --include="*.md" .` should return nothing. |
| 16 | `docs/HARNESS-EVALUATION.md` (meta, surfaced via item 12's run) | Pending | — | — | — | Add this document to the blind-judge exclusion list for future harness-eval runs. | Both a Track B and a Track C judge incidentally read this document while grepping for callers (it documents prior runs' answers, e.g. a plant's designed answer). Scores were reportedly derived independently and matched independent repo evidence every time observed so far, but this is a live risk for future runs — a judge could crib an intended answer instead of deriving one. | When dispatching future harness-eval judge subagents on this repo, explicitly instruct them to exclude `docs/HARNESS-EVALUATION.md` from any grep/read used to inform their verdict. | Spot-check a future run's judge transcripts to confirm this file wasn't read, or wasn't relied on if read incidentally. |

## Skills (`skills/<name>/`)

Generated 2026-09-12 by running the `harness-eval` skill, Track A+B+C, against every local skill under `skills/` (11 skills) — one fully isolated run per skill (`inventory_extract.py --seed skills/<name>/SKILL.md`, no pooling across skills), each with its own dual-judge Track B and Track C. Q1 (optional docs) was answered `skill-tree only` for every run, so each skill's own `references/*.md` and `templates/*.md` cites were **excluded** from Track B/C scoring below — several rows independently rediscovered the same root cause already logged above: `doc_scope.py`'s skill-tree recognition is hardcoded to `.agents/skills/` / `.cursor/skills/` / `.claude/skills/` and doesn't recognize this repo's actual bare `skills/<name>/` layout, so a skill's own load-mandated `references/` files land in "optional docs" instead of always-in-scope T2 (worst affected: `session-evaluate`, `tests-code-review`, `code-review`, `build-feature`). Every trap-gate FAIL noted below was a first-pass artifact of the same systemic cause documented above (calibration plants cite `AGENTS.md`/`.agents/skills/workflow-tips/SKILL.md`, which don't exist in this repo) and was resolved via a rescore per `PROTOCOL.md` before the row's verdict was recorded, so every Track B/C verdict below is **certified** (trap PASS at merge time).

**One row per actionable item** (not per skill) — where a skill's run surfaced more than one action, it's split across multiple numbered rows so each can be tracked and referenced independently by `#`.

| # | Name of the skill / template | Status | Track A | Track B | Track C | Bottom line | Description | How to Fix | How to validate |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `architecture-evaluate` | Done | — | **Ship** — `C124`–`C142` (15 claims), dual REDUNDANT-GENERAL, cost 0 | Corroborating: J1 (MIXED overall) independently cut the same block; J2 (KEEP-CORE overall) separately flagged it as slimmable padding | Compress the 3 worked-example traces into one short illustrative example | `SKILL.md` (~lines 124–142) carries three fully-narrated walkthroughs — a Go `auth.go` incremental-update trace, a Magento `Shipping` package-mode trace, and a Go `internal/notifications` trace — that just replay steps already specified earlier in the file with fabricated concrete values. Both Track B judges scored the whole block REDUNDANT-GENERAL at cost 0; both Track C judges independently flagged it too despite disagreeing on the file's overall verdict (Hold). | Rewrite the three traces into one compact example or a mode→step-delta table. Do not touch the mode-routing, guardrail, or 9-file-schema sections — Track C confirmed these are hard-loaded by `skills/code-review/SKILL.md`, `skills/tests-code-review/SKILL.md`, `templates/reference-loading-constraint.md`, and `templates/test-execution-scope.md`. | Diff the Examples section word count before/after to confirm compression; confirm the routing/guardrail/schema sections are byte-identical to before; `grep -rl` the `docs/codebase/*.md` filenames across the 4 confirmed citers to reconfirm they still resolve; re-run `track_a_correctness.py` — should stay clean. |
| 2 | `architecture-evaluate` | Done | Dead-link risk (not yet broken): `SKILL.md:85,686,845` cite `docs/TECH_DEBTS.md`, which doesn't exist in this repo yet | — | — | Remove the `TECH_DEBTS.md` cites entirely — the file is not in use | Per explicit user instruction, treated as fully dead rather than a silent-break risk to caveat: `docs/TECH_DEBTS.md` (and its `tech-debt-report`/`TD-XX` ledger) is not in use in this repo, so the three cites (Shared Guardrails out-of-scope bullet, Step 11 Tone instruction, Step 6 Holistic Sweep do-not-modify list) were deleted outright rather than annotated. | Removed each of the 3 citing clauses (lines 85, 686, 845) in full, including the now-meaningless `TD-XX` ledger sentence in the Shared Guardrails bullet, so no dangling reference to the removed file/ledger remains anywhere in `SKILL.md`. | `grep -n "TECH_DEBTS" skills/architecture-evaluate/SKILL.md` should return 0 matches; re-run `track_a_correctness.py` seeded on this skill — the dead-link risk finding should no longer appear. |
| 3 | `architecture-evaluate` | Done | — | **Ship** — `C104` (secrets/tokens line, near-dup of `C015`), `C105` (generic "only include sections with evidence" platitude) | — | Delete the two duplicate/generic lines | `C104` restates `C015`'s fuller "never write secret values" statement two screens earlier in the same file; `C105` is a content-free authoring platitude with no repo-specific delta. Both dual-judge REDUNDANT. | Delete `C104` entirely; delete or substantially tighten `C105`. | `grep` for `C104`'s old wording to confirm removal; confirm the file still reads correctly around both cut points. |
| 4 | `build-feature` | Done | — | **Ship** — `C016`, dual REDUNDANT-CODE, cost ≤1 | Keep-core (S001) unaffected — this is a surgical trim inside an otherwise essential file | Replace the inline `progress.mjs` usage block with a pointer | The "State ownership" section's `node scripts/progress.mjs <path> --init ...` full CLI usage text duplicates `scripts/progress.mjs`'s own header docstring verbatim — cheaply rediscoverable by reading the script. | Replace the inline usage block with a short pointer ("see `scripts/progress.mjs`'s own usage header"); keep only the behavioral rule (use the script, never hand-edit `progress.md`). | Diff the "State ownership" section before/after — confirm it no longer reprints the script's own `Usage:` docstring; re-run `track_a_correctness.py` — should stay clean. |
| 5 | `build-feature` | Done | — | Hold — `C011`/`C012` (routing lines vs. `fix-review`/`architecture-evaluate`'s own frontmatter), `C033`/`C038`/`C099` (template-delegation lines vs. the linked templates' own text), `C046` (secrets caveat vs. root `CLAUDE.md`) | — | Human review: decide whether these 6 claims are useful routing cues or pure duplication | Judges disagreed (KEEP-ROUTING/KEEP-POLICY vs. REDUNDANT-GENERAL) on whether restating a rule that already lives verbatim in a linked template/skill's own frontmatter earns its keep as a routing cue. | No auto-fix — have a human read each of the 6 claims against its linked source and decide keep-as-cue vs. cut-as-duplicate. | Record the decision per claim; if any are cut, re-run `track_a_correctness.py` to confirm no dangling reference. |
| 6 | `code-review` | Done | — | Hold (both judges agree compressible, disagree how far) — `C127`–`C163`, 37 claims | Keep-core (S001) unaffected | Compress the 37-row Examples walkthrough | The entire Examples section re-narrates Steps 1–9, which are already fully specified earlier in the same file. | Compress into a mode→step-delta table instead of full replays. | Diff the Examples section length before/after; confirm the step-delta table still covers every mode `code-review` supports. |
| 7 | `code-review` | Done | — | Corroborated duplicate (Hold-banded but verbatim): `C076`–`C082` ("Reviewer Stance") = `C002`–`C008`, confirmed via `grep -n "You are the villain"` hitting lines 37 and 429 | — | Delete the duplicate "Reviewer Stance" block | The same block is repeated verbatim at two line numbers in the same file — not a judgment call. | Delete the block at `SKILL.md:429`; keep only the one at line 37 (or replace `:429` with a one-line pointer). | `grep -c "You are the villain" skills/code-review/SKILL.md` should return 1, not 2. |
| 8 | `code-review` | Done | — | **Ship** — `C001`, dual REDUNDANT | — | Rephrase the opening line | The opening sentence restates the frontmatter `description` almost verbatim. | Rephrase or cut the opening line so it doesn't restate frontmatter. | Diff the line — confirm it no longer near-matches the frontmatter `description`. |
| 9 | `code-review` | Done | — | Corroborated duplicate (Hold-banded): `C011`'s trigger list = frontmatter `metadata.triggers` array | — | Single-source the trigger list | The Performance Audit trigger phrase list is an exact duplicate of the frontmatter triggers array. | Replace the inline trigger-phrase list with a reference to the frontmatter `metadata.triggers` array. | `grep -n` for the phrase list body — should exist only in frontmatter afterward. |
| 10 | `code-review` | Done | — | Corroborated duplicate (Hold-banded): `C061` = `skills/tests-code-review/SKILL.md:308` verbatim; `C066` = `skills/code-review/STATE.md` AD-002 stats | — | Single-source the two cross-file duplicates | Guardrail text and cost/percentage stats are copied verbatim from sibling-skill/decision-log files — a drift risk if one is updated without the other. | For each of `C061`/`C066`, pick one canonical location (this file vs. `tests-code-review/SKILL.md:308` / `STATE.md` AD-002) and point the other at it. | Confirm the figures/text are defined in exactly one place; the other should be a pointer, not a copy. |
| 11 | `complete-review` | Done | — | **Ship** — 22 claims (`C001`, `C108/C113/C126/C133/C138/C143`, 14 mode-detection one-liners, plus `C112`'s first instance) | Keep-core (S001) unaffected — trim confined to Examples scaffolding | Collapse 5 of the 11 Examples into one-line traces | Examples 1, 2, 6, 7, 8 are mechanical one-line replays of a rule already stated once in Step 1 — cheap to rediscover, no independent information. | Rewrite Examples 1, 2, 6, 7, 8 to a single line each ("Example N: `<input>` → `<mode>`, proceeds as Step X"); leave Examples 5, 10, 11 as full walkthroughs (both judges scored them KEEP-COMPRESSED). | Confirm Examples 1/2/6/7/8 are now one line each while 5/10/11 remain full walkthroughs; re-run `track_a_correctness.py` — should stay clean. |
| 12 | `complete-review` | Done | — | **Ship** — `C112` (report-string template) repeated verbatim at `C083/C121/C125/C156` | — | Single-source the report-string template | The same report-string phrasing template is repeated verbatim across 5 locations in the same file. | Keep one canonical template (e.g. in Step 3); have the other 4 locations reference it by name instead of reprinting the full string. | `grep -c` a unique substring of the template — should return 1 after the fix, not 5. |
| 13 | `disk-evaluate` | Done | — | **Ship** — `C001` (`SKILL.md:13`), dual REDUNDANT, dup of frontmatter `description` | — | Delete the duplicate opening line | Line 13 duplicates the frontmatter `description` field verbatim. | Delete `SKILL.md:13` (or fold any non-duplicate nuance into the frontmatter description only). | Confirm line 13 is gone; re-run `track_a_correctness.py` — should stay clean. |
| 14 | `disk-evaluate` | Done | — | **Ship** — `C011` (`SKILL.md:51`), dual REDUNDANT, bare example line | — | Delete or replace the bare example line | `SKILL.md:51`'s `User: /disk-evaluate` line adds nothing beyond the skill's own name; the example at `:55` already does this better. | Delete `SKILL.md:51`, or replace it with a substantive example. | Confirm line 51 is gone/replaced; re-run `track_a_correctness.py` — should stay clean. |
| 15 | `disk-evaluate` | Pending | — | — | Hold (disagree) — J1 KEEP-CORE, J2 MIXED, on Examples/Troubleshooting sections | Re-judge Track C on a second model before touching Examples/Troubleshooting | J2 ties the destructive-command guardrails to a real 2026-09-03 incident in `STATE.md` (KEEP) but flags the Examples/Troubleshooting sections as restating Step 2/Step 4 without new decision logic (SLIM); J1 calls the whole file KEEP-CORE. | No auto-fix — re-run Track C Judge1/Judge2 on a second model family; act only if that run also lands MIXED or also lands KEEP-CORE (agreement either way settles it). | Compare the second-model run's verdict against this one; proceed with a trim only on agreement. |
| 16 | `fix-review` | Done | — | Corroborated duplicate (Hold-banded): Guardrails' short-form Jira Ticket Sync rules (`C042`–`C047`) ≈ the fuller dedicated "Jira Ticket Sync" section (`C097`–`C105`) | — | Consolidate the Jira Ticket Sync duplication | The same 6 rules are stated twice in the file — a short form in Guardrails, a full form later. | Remove the short-form rules from Guardrails; replace with a pointer to the full "Jira Ticket Sync" section. | `grep -c` the Jira Ticket Sync rule text — should appear once, not twice. |
| 17 | `fix-review` | Done | — | Corroborated duplicate (Hold-banded): `C033` ≈ `C095` (Agent Wait Protocol repeated) | — | Consolidate the Agent Wait Protocol repeat | The same cross-ref is stated twice in the file. | Remove `C033`; keep the canonical mention at `C095`, or replace `C033` with a pointer. | `grep -c` the Agent Wait Protocol reference text — should appear once. |
| 18 | `fix-review` | Done | — | **Ship** — `C001`, dual REDUNDANT | — | Trim the H1 intro | The intro paragraph restates the frontmatter `description` almost verbatim. | Trim the H1 intro paragraph so it doesn't restate frontmatter. | Diff the intro — confirm it no longer near-matches frontmatter. |
| 19 | `fix-review` | Done | — | **Ship** — `C010`, dual REDUNDANT, cost ≤1 | — | Cut or shorten the comments rule | "No explanatory comments unless non-obvious" duplicates the user's own global `CLAUDE.md` Coding Style directive. | Cut `C010`, or shorten to a one-line reminder. | Diff the section — confirm the full rule isn't restated (it's already project-wide policy). |
| 20 | `fix-review` | Done | — | — | Secondary note (within overall Keep-core): both judges flagged Examples 6 and 8 as re-narrating guardrails already stated in prose | Human skim of Examples 6 and 8 for compression | These two examples restate guardrails the file already states in prose, unlike the other 7 examples. | Have a human read Examples 6 and 8 and compress any pure restatement, keeping the concrete walkthrough value. | Confirm Examples 6/8 are shorter but still demonstrate their guardrail in action. |
| 21 | `not-your-babysitter` | Rejected | — | Sub-finding (not Ship-eligible): `C002/C008/C022/C049` (lines 16/24/51/114) — both judges agree these list-lead-ins add no content beyond introducing their own list | — | (Optional) fold the 4 list-lead-in sentences into their headings | Low-value, low-risk prose tightening; no information is lost either way. | Fold each of the 4 lead-in sentences into its list heading. | Diff — confirm the 4 lines are folded and list content is intact. |
| 22 | `not-your-babysitter` | Rejected | — | — | Hold (disagree) — J1 KEEP-CORE, J2 MIXED, on `lines 32–49` and `81–92` overlapping the user's global `CLAUDE.md` (§Confidence Threshold, §Verification Before Done/§No Laziness) | Re-judge Track C on a second model before touching `lines 32–49`/`81–92` | J2's case is well-evidenced (specific CLAUDE.md section names cited); J1 disagrees. Genuine disagreement, correctly Held. | No auto-fix — re-run Track C Judge1/Judge2 on a second model family; if confirmed, compress to a one-line pointer at the CLAUDE.md rule and keep only skill-specific deltas (numeric-figures-must-cite-source; protected-things checklist; size-of-change heuristic). | Act only on cross-model agreement. |
| 23 | `qa-steps` | Done | — | **Ship** — `C032` (`SKILL.md:124`), dup of Step 1's `C004/C005` | — | Cut/compress the Example 3 Actions line | Near-duplicate of Step 1's "no ticket → ask, never guess" rule. | Cut or compress `SKILL.md:124` to a pointer at Step 1. | Diff — confirm it no longer restates Step 1 verbatim. |
| 24 | `qa-steps` | Done | — | **Ship** — `C033` (`SKILL.md:130`), dup of Step 2's `C008` | — | Cut/compress the "Jira MCP not connected" troubleshooting line | Near-duplicate of Step 2's stop/no-fabricate rule. | Cut or compress `SKILL.md:130` to a pointer at Step 2. | Diff — confirm it no longer restates Step 2 verbatim. |
| 25 | `qa-steps` | Done | — | **Ship** — `C036` (`SKILL.md:142`), dup of Step 7's `C026` | — | Cut/compress the "Posting to Jira fails" troubleshooting line | Near-duplicate of Step 7's posting-failure rule. | Cut or compress `SKILL.md:142` to a pointer at Step 7. | Diff — confirm it no longer restates Step 7 verbatim. |
| 26 | `session-evaluate` | Done | — | Hold — `C001` (`SKILL.md:12`), J1 KEEP-ROUTING vs J2 REDUNDANT-CODE, near-dup of frontmatter `description` | — | Replace the intro paragraph with a distinct tagline | The body intro under "# Session Evaluate" nearly duplicates the file's own YAML `description`. | Replace `SKILL.md:12`'s intro paragraph with a one-line tagline distinct from frontmatter. | Diff — confirm the new line doesn't near-match frontmatter `description`. |
| 27 | `session-evaluate` | Done | — | Hold — `C046` (`SKILL.md:177`), J1 REDUNDANT-GENERAL (dup of root `CLAUDE.md`'s Skill Modification Rules) vs J2 KEEP-ROUTING | — | Human call: pointer vs. local restatement for the Skill Modification Rules line | Same CLAUDE.md-overlap pattern flagged in `not-your-babysitter` row 22 — a recurring cross-skill signal. | If a pointer is preferred, replace `SKILL.md:177` with a one-line reference to root `CLAUDE.md`'s Skill Modification Rules section. | Confirm `CLAUDE.md` is loaded whenever this skill runs (it is, globally) before relying on a pointer alone. |
| 28 | `session-evaluate` | Done | — | Hold — `C085` (`SKILL.md:308`), J1 REDUNDANT-GENERAL (dup of root `CLAUDE.md`'s Change Request Workflow/commit rules) vs J2 KEEP-POLICY | — | Human call: pointer vs. local restatement for the Change Request Workflow line | Same CLAUDE.md-overlap pattern as row 27. | If a pointer is preferred, replace `SKILL.md:308` with a one-line reference to root `CLAUDE.md`'s Change Request Workflow section. | Confirm `CLAUDE.md` is loaded whenever this skill runs before relying on a pointer alone. |
| 29 | `session-evaluate` | Pending | Scope gap: `doc_scope.py` misclassified this skill's own load-mandated `references/findings-catalog.md` and `references/example-report.md` as optional docs | — | — | Re-run with the two references files included | This skill's own `SKILL.md` mandates reading both files in full, but the skill-tree-only Q1 answer excluded them from Track B/C scoring. | Re-run: `inventory_extract.py --seed skills/session-evaluate/SKILL.md --include-doc skills/session-evaluate/references/findings-catalog.md --include-doc skills/session-evaluate/references/example-report.md`. | The follow-up run should produce Track B/C coverage for both files. |
| 30 | `tech-reference-add` | Done | — | **Ship** — `SKILL.md:43`, dual REDUNDANT-GENERAL | — | Delete the generic parsing line | "Parse from the user's message:" is generic instruction-parsing framing any agent already does. | Delete `SKILL.md:43`. | Confirm the line is gone and the surrounding instruction still reads correctly. |
| 31 | `tech-reference-add` | Done | — | **Ship** — `SKILL.md:48`, dual REDUNDANT-GENERAL | — | Delete the generic clarifying-question line | "Ask clarifying questions before proceeding if any of the above is unclear" is a standard agent default, not a skill-specific rule. | Delete `SKILL.md:48`. | Confirm the line is gone and the surrounding instruction still reads correctly. |
| 32 | `tech-reference-add` | Done | — | **Ship** — `SKILL.md:90`, dual REDUNDANT-GENERAL | — | Delete or merge the generic iteration line | "For each confirmed target file:" is generic iteration scaffolding, not a distinct instruction. | Delete `SKILL.md:90`, or merge it into the loop body it introduces. | Confirm the line is gone/merged and the loop body still reads correctly. |
| 33 | `tech-reference-add` | Pending | — | Hold — `SKILL.md:25`, J1 KEEP-POLICY vs J2 REDUNDANT-CODE (cost 1) | — | Human review: write-scope guardrail | "Do NOT write outside `skills/` or `extended/` directories" — one judge calls it a real guardrail worth stating explicitly, the other calls it cheaply rediscoverable from repo structure. | No auto-fix — have a human decide keep vs. cut. | Record the decision; no file change unless cut is chosen. |
| 34 | `tech-reference-add` | Pending | — | Hold — `SKILL.md:31,54`, J1 KEEP-COMPRESSED vs J2 REDUNDANT-CODE (cost 0) | — | Human review: "may be covered by training data" clarifying question | One judge sees this as the only place steering this specific user prompt; the other calls it redundant. | No auto-fix — have a human decide keep vs. cut. | Record the decision; no file change unless cut is chosen. |
| 35 | `tests-code-review` | Pending | — | **Ship** — `C127`–`C131` (`SKILL.md:568-573`, "Key Reminders" footer), dual REDUNDANT | Keep-core (S001) unaffected — trim confined to the footer | Delete or compress the "Key Reminders" footer | Pure software-engineering truisms with zero repo-specific content, cheaply rediscoverable. | Delete `SKILL.md:568-573`, or compress to a single line if kept at all. | Diff — confirm the footer is gone/compressed. |
| 36 | `tests-code-review` | Pending | — | Hold (structural disagreement) — `C096`–`C124` (Examples), J1 KEEP-COMPRESSED/KEEP-ROUTING vs J2 mostly REDUNDANT-GENERAL | — | Re-judge Track B on a second model for the Examples section | This is a real structural disagreement (not noise) on whether the 3 worked examples add value beyond the step-by-step spec. | No auto-fix — re-run Track B Judge1/Judge2 on a second model family for this section specifically. | Act only on cross-model agreement. |
| 37 | `tests-code-review` | Pending | Coverage gap: Track A doesn't scan the 4 `../../templates/*.md` cites (manually verified all exist) | — | — | Re-run with the checklist/template dependencies included | This skill's own `references/test-review-checklist.md` and 4 `templates/*.md` files were excluded from Track B/C by the skill-tree-only scope. | Re-run: `inventory_extract.py --seed skills/tests-code-review/SKILL.md --include-doc-type skills --include-doc-type templates`. | The follow-up run should produce Track B/C coverage for the checklist/template files; also `grep -rn "anchor" skills/complete-review/SKILL.md skills/fix-review/SKILL.md` to reconfirm the fan-in claim before any future Slim consideration on this file overall. |

## Root Context Files

Generated 2026-09-12 in a separate `harness-eval` pass, Track A+B+C, seeded on the two root T0 files only
(`--seed CLAUDE.md --seed CLAUDE.global.md`; run id `2026-09-12-claude-md-only`). Optional docs (`config/skills.json`,
`docs/CLI.md`, `docs/SKILL-ADR.md`, `docs/codebase/*`) were offered via harness-eval's Q1 gate and **declined** for
this pass, so T1/T2 are both 0 — only the two root files themselves were scored. Claims C001–C050 belong to
`CLAUDE.global.md`; C051–C076 belong to `CLAUDE.md`. Track C scores each file as one whole surface (S001 =
`CLAUDE.global.md`, S002 = `CLAUDE.md`).

**Trap-gate note:** Track B's trap gate failed here too (misses: P005, P006 — the two fixed KEEP plants), for the
same systemic reason already logged above — the plant text references `AGENTS.md` / `.agents/skills/workflow-tips/SKILL.md`,
paths this repo doesn't have, which pulled Judge2 toward REDUNDANT on plants meant to read as universally KEEP.
Track C's trap gate passed (1/3 miss), but the one miss is a *new* variant of the same category: plant `S903`
("Module Boundary Rule") assumes a modules/repository/transaction-boundary layered architecture — both judges
independently confirmed via repo-wide search that no such layer exists anywhere in this flat, `.md`-heavy repo, so
scoring it SLIM was the locally-correct call even though the plant's designed answer was KEEP-CORE. Track B's Ship
band is fully discarded for this run (per the `misses ≤ 1` rule); Track C's Slim/Keep-core/Mixed bands are not
discarded, but both real surfaces landed **Hold** on judge disagreement anyway (see below), so no certified action
exists yet either way.

Each row below is one standalone actionable item derived from the findings (multi-finding files split one item per line), numbered in `#` for reference.

| # | Name of the skill / template | Status | Track A | Track B | Track C | Bottom line | Description | How to Fix | How to validate |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `CLAUDE.global.md` | Pending | BROKEN — `docs/codebase/PIPELINE.md` cited, file doesn't exist | — | — | Fix the dead `PIPELINE.md` cite in the Session Start table. | The "Session Start — Project Context" table cites `docs/codebase/PIPELINE.md`; `docs/codebase/` currently ships only 8 files (ARCHITECTURE, CONCERNS, CONVENTIONS, INTEGRATIONS, PROJECT, STACK, STRUCTURE, TESTING) — no PIPELINE.md. Confirmed with `test -f docs/codebase/PIPELINE.md`. | Point the table row at an existing file, remove the row until `architecture-evaluate` generates `PIPELINE.md`, or trigger that skill to create it. | Re-run `python3 "$SKILL_DIR/scripts/track_a_correctness.py" --root . --run-id <new-run> --seed CLAUDE.md --seed CLAUDE.global.md` — expect 0 broken. |
| 2 | `CLAUDE.global.md` + `CLAUDE.md` | Pending | — | Trap gate FAILED (misses: P005, P006) — Ship band fully discarded for this run | — | Re-run Track B with a corrected KEEP-plant set before trusting item 8 (or any other Track B finding) as certified. | Both fixed KEEP plants (secrets policy, local-vs-CI env caveat) were scored REDUNDANT by Judge2 because the plant text itself references `AGENTS.md` / `.agents/skills/workflow-tips/SKILL.md`, paths this repo doesn't have — the same systemic issue already logged for other rows in this file. Per protocol, `misses ≤ 1` is required to PASS; this run had 2. | Rescore P005/P006 on content merits only per `PROTOCOL.md`'s troubleshooting guidance (as already done for the `Skills` section rows above), then re-run `merge_agreement.py`. | `07-agreement.md` should read `Trap gate: PASS` after rescoring; only then treat item 8 below as a certified Ship. |
| 3 | `CLAUDE.global.md` | Pending | — | Hold (disagree) — C016 | — | Decide whether to keep, cut, or fold the "No Laziness" line into its neighbor. | C016 ("**No Laziness**: Find root causes. No temporary fixes. Senior developer standards.") — J1 scored KEEP-POLICY, J2 scored REDUNDANT-GENERAL (cost 0, reads it as generic engineering advice restatable without the file). | Human call: keep as-is, or fold into the neighboring "Autonomous by default" bullet to remove the standalone restatement. | If edited, re-run Track B on this claim (or the whole file) and confirm the new phrasing doesn't reintroduce the same generic-restatement flag. |
| 4 | `CLAUDE.global.md` | Pending | — | Hold (disagree) — C021/C022/C025/C026 | — | Remove or simplify the decorative tier-banner headers/separator lines — neither judge found instructional content in them. | `TIER 2 · SESSION START` / `TIER 3 · TOOL USE` banner headers and the `═══...` separator lines — J1 scored UNCLEAR, J2 scored REDUNDANT-GENERAL; they disagreed only on which non-KEEP bucket to file them under, not on whether they carry content. | Delete the banner/separator lines, or replace with plain markdown `##` headings if the visual section break is still wanted. | Re-run Track A (should stay clean) and confirm the file's section structure still reads clearly without the ASCII banners. |
| 5 | `CLAUDE.global.md` | Pending | — | Hold (disagree) — C033 | — | Decide whether the Context7 MCP paragraph stays as prose or is trimmed to a one-line routing pointer. | C033 ("Context7 MCP (`mcp__context7__*`) is available for fetching up-to-date documentation...") — J1 scored REDUNDANT-CODE (duplicates the agent's own tool manifest), J2 scored KEEP-ROUTING (useful as an explicit "use this over web search" policy statement). | Human call: keep as a routing/policy statement (J2's reading), or shorten to a one-line pointer since the tool's existence is already visible in the manifest (J1's reading). | If shortened, confirm the routing intent ("use Context7 before web search for library docs") still survives the cut. |
| 6 | `CLAUDE.md` | Pending | — | Hold (disagree) — C051 | — | Decide whether the `docs/codebase/PROJECT.md` routing sentence stays as-is or is dropped as duplicative. | C051 ("See `docs/codebase/PROJECT.md`... for the full agent context set") — J1 scored KEEP-ROUTING (necessary pointer), J2 scored REDUNDANT-CODE (duplicative of `CLAUDE.global.md`'s own Session Start table, item 1's file). | Human call: keep (only routing sentence in this file pointing at docs/codebase) or drop as duplicative. | If dropped, confirm `docs/codebase/*` files are still discoverable from `CLAUDE.global.md`'s Session Start table alone before removing this second pointer. |
| 7 | `CLAUDE.md` | Pending | — | Hold (disagree) — C052 | — | Decide whether the "Project Nature" intro line stays as prose. | C052 ("This is **not an implementation-heavy codebase**...") — J1 scored REDUNDANT-CODE (restates what the repo layout already shows), J2 scored KEEP-POLICY (sets a behavioral rule — don't apply typical refactor/DRY/test-coverage heuristics to `.md` files — not recoverable from the file tree alone). | Human call: keep (J2's behavioral-rule reading is recommended — the actual instruction isn't visible from listing the repo). | No mechanical check; if kept, mark Rejected rather than Done if a future pass reaches the same conclusion. |
| 8 | `CLAUDE.md` | Pending | — | Dual REDUNDANT-CODE (C067, C069–C071) — blocked from Ship only by item 2's trap failure | — | Trim the `fs-harness add/delete/override` command bullets; replace with a single pointer to `docs/CLI.md`. | C067 ("Skills are managed by the `fs-harness` script...") and C069/C070/C071 (the `add`/`delete`/`override` one-line command reminders) duplicate `docs/CLI.md`'s own Commands table verbatim — the same paragraph in `CLAUDE.md` already calls `docs/CLI.md` "authoritative." Highest-confidence redundancy signal in this run; Hold only because of item 2's trap failure, not disagreement. | Replace the 3 inline command bullets with a single sentence pointing at `docs/CLI.md`'s Commands table (an unlinked mention of `add`/`delete`/`override` is fine; drop the syntax). | Confirm `docs/CLI.md`'s Commands table still documents `add`/`delete`/`override` with the same flags, then re-run Track A — expect 0 new broken cites. Do this only after item 2 confirms trap PASS. |
| 9 | `CLAUDE.md` | Pending | — | — | Flagged independently by both usefulness judges as stale | Correct the stale `to-prd` claim in the "Known Limitation" section. | The sentence "It is still registered in `config/skills.json`/installed locally" no longer holds — both Track C Judge1 and Judge2 independently confirmed via direct inspection that `config/skills.json` has no `to-prd` entry. | Update or remove the "still registered in config/skills.json" clause; confirm the rest of the Known Limitation note (upstream removal, `to-spec` as closest match) is still accurate. | `grep -c '"to-prd"' config/skills.json` should return 0 (already true) and the corrected sentence should no longer claim registration. |
| 10 | `CLAUDE.global.md` + `CLAUDE.md` | Pending | — | — | Hold (disagree) — S001/S002 KEEP-CORE vs MIXED swap | Re-run Track C usefulness on a second model family before treating either file's Keep-core/Mixed split as resolved. | Judge1 called `CLAUDE.global.md` KEEP-CORE / `CLAUDE.md` MIXED; Judge2 called the reverse. Both judges used the same model (`claude-sonnet-5`) — per harness-eval's model-sensitivity rule, a cross-model check is needed before any Slim/Mixed action. | Re-run Steps 7–10 (Track C) seeded on `CLAUDE.md`/`CLAUDE.global.md` with a different allowlisted model family for both judges; compare against this run's result. | If the second model agrees with one of the two existing verdicts on both files, that becomes the working call; if it disagrees with both, leave as Hold and note the 3-way split. |
| 11 | `CLAUDE.global.md` | Pending | — | — | MIXED slim-cell candidate (Judge2 only) — conditional on item 10 | If item 10 confirms `CLAUDE.global.md` as MIXED, cut the identified generic/theory bucket. | Judge2's MIXED slim cell for `CLAUDE.global.md` named a specific bucket with no found repo-specific consumer: Collaboration Mindset, Core Principles, Coding Style, the k3s infra-default rule, the Playwright cache-path note, Skill Creation Policy, File Deduplication. Not actionable yet — Judge1 called the whole file KEEP-CORE instead (item 10's disagreement). | Do not cut yet — dependent on item 10's second-model re-run confirming MIXED (not KEEP-CORE) for this file. | Only proceed once item 10 resolves in favor of MIXED; then keep behavior-changing rules and cut the named bucket, and re-run Track A after any structural edit. |
